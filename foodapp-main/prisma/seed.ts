/**
 * Prisma seed placeholder.
 * Run SQL seed scripts via `npm run db:seed` in this offline scaffold.
 * Once Prisma packages are installed, this file can be switched to Prisma client seeding.
 */
export {};

